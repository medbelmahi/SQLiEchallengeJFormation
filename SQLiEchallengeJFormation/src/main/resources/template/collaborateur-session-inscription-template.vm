<html>
	<head>
		<title>Example</title>
	</head>

	<body >
		<h1>Bonjour ${model.models.get(0)}</h1>
		<p>Session ${model.models.get(1)}</p>
		<a href="http://${model.models.get(2)}:${model.models.get(3)}/${model.models.get(4)}/collaborateur/session/inscription/display?code=${model.models.get(5)}">Inscription</a>
		<p>JFormation | SQLi | Echallenge</p>
	</body>
</html>